using MediatR;

namespace SaysanPwa.Domain.SeedWorker;

public abstract class Entity 
{
    /*private List<INotification> _notifications = null!;

    public IReadOnlyCollection<INotification>? Notifications => _notifications?.AsReadOnly();


    public virtual void AddDomainEvent(INotification notification)
    {
        if (_notifications == null)
        {
            _notifications = new();
        }

        _notifications.Add(notification);
    }


    public virtual void ClearDomainEvents()
    {
        if (_notifications != null)
        {
            _notifications.Clear();
        }
    }*/
}
